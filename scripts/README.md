# Scripts

This folder is reserved for future ingestion utilities.

Recommended next script:

1. Read `data/rag_chunks_redacted.jsonl`.
2. Generate embeddings with OpenAI.
3. Insert rows into `public.business_plan_chunks`.
4. Keep `source_file` and raw chunk text server-only.
